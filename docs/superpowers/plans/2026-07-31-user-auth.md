# Autenticação de usuário + sincronização de progresso — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir o `ACCESS_PASSPHRASE` compartilhado por contas individuais (email+senha) via Supabase, com cadastro por código de convite (código solto ou link pré-preenchido), sincronização de `UserProgress` entre `localStorage` e a nuvem, migração automática do progresso local existente, e uma tela de admin para gerar convites.

**Architecture:** Front-end estático (React 19 + Vite 6) continua no GitHub Pages, sem servidor próprio. Supabase fornece Auth (email+senha), Postgres com Row Level Security, e duas Edge Functions (`validate-invite-and-signup`, `admin-generate-invite`) para a lógica que não pode rodar no navegador (validar/consumir código de convite). Progresso passa a sincronizar via um ponto único já existente em `App.tsx` (`updateProgress`).

**Tech Stack:** React 19, TypeScript, Vite 6, `@supabase/supabase-js`, Supabase (Auth + Postgres + Edge Functions), GitHub Actions (deploy existente), Tailwind v4 (estilos já usados no projeto).

## Global Constraints

- Sem suíte de testes no projeto — verificação é `npx tsc --noEmit` (lint) + `npm run build`, conforme `CLAUDE.md`. Cada task usa esses comandos como gate, não testes automatizados.
- Toda alteração de progresso deve continuar passando por `updateProgress` em `src/App.tsx:174` — não duplicar o ponto de escrita.
- Não remover a lógica de "senha inline no bundle é fraca" do `ADMIN_PASSPHRASE` — ele permanece exatamente como está (`src/App.tsx:92`), só ganha uma ação nova.
- Push direto para `main` após build limpo — sem branch de staging (workflow da dona, ver `CLAUDE.md`).
- Toda tela nova (login, cadastro, admin de convites) passa pela skill `frontend-design` antes de codar o JSX/CSS final — pedido explícito da dona.
- Repo local: `~/Projetos/rclsampaio-jpg.github.io` (clone de `rclsampaio-jpg/rclsampaio-jpg.github.io`, branch `main`).

---

## File Structure

**Novos arquivos:**
- `src/lib/supabase.ts` — cliente Supabase único (`createClient`), usado por todo o app.
- `src/contexts/AuthContext.tsx` — `AuthProvider` + hook `useAuth()`: usuária atual, `signIn`, `signUp`, `signOut`, estado de loading.
- `src/hooks/useProgressSync.ts` — hook que envolve leitura/escrita de `UserProgress`, sincronizando `localStorage` ↔ Supabase (debounced) e fazendo a migração de progresso local na primeira sincronização.
- `src/components/auth/LoginView.tsx` — tela de login (email+senha).
- `src/components/auth/SignupView.tsx` — tela de cadastro (email+senha+código, lê `?codigo=` da URL).
- `src/components/auth/InviteAdminPanel.tsx` — painel de geração de convites (gated pelo `ADMIN_PASSPHRASE` existente).
- `supabase/migrations/0001_init.sql` — schema (`profiles`, `user_progress`, `invite_codes`) + RLS.
- `supabase/functions/validate-invite-and-signup/index.ts` — Edge Function de cadastro.
- `supabase/functions/admin-generate-invite/index.ts` — Edge Function de geração de convite.

**Modificados:**
- `src/App.tsx` — remove o gate de `ACCESS_PASSPHRASE` (linhas 58–83), envolve a árvore com `AuthProvider`, troca `updateProgress` para usar `useProgressSync`, adiciona a rota de admin para `InviteAdminPanel`.
- `.github/workflows/deploy.yml` — injeta `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY` como env vars no step de build, lidas de GitHub Secrets.
- `package.json` — adiciona `@supabase/supabase-js`.

**Removidos:**
- `src/services/firebaseAdapter.ts` — esqueleto nunca ativado (`USE_FIREBASE_BACKEND = false`, SDK real todo comentado); confunde com a abordagem real (Supabase) se ficar no repo.

---

### Task 1: Provisionar o projeto Supabase (infraestrutura, sem código)

**Files:** nenhum arquivo de código — só configuração externa + registro dos valores obtidos.

**Interfaces:**
- Produces: `SUPABASE_URL` e `SUPABASE_ANON_KEY` (valores reais, a serem usados nos Secrets do GitHub e no `.env.local` de desenvolvimento nas próximas tasks).

- [ ] **Step 1: Abrir o signup do Supabase no navegador**

Navegar (Playwright) para `https://supabase.com/dashboard/sign-up`. **PARAR** — a dona cria a conta (idealmente login via GitHub, já autenticado) e confirma quando estiver logada no dashboard.

- [ ] **Step 2: Criar um novo projeto**

Clicar em "New project", nomear `renaser`, escolher região mais próxima do Brasil (`South America (São Paulo)` se disponível), gerar/guardar a senha do banco (Supabase pede uma senha de Postgres — guardar num gerenciador de senhas, não vai ser usada em código). **PARAR** para a dona confirmar a criação (é uma ação que provisiona recursos reais, ainda que no free tier).

- [ ] **Step 3: Copiar URL e anon key**

Em Project Settings → API, copiar `Project URL` (→ `SUPABASE_URL`) e `anon public` key (→ `SUPABASE_ANON_KEY`). Colar os dois valores no chat pro agente registrar (não são segredos sensíveis — a anon key é pública por design, protegida por RLS, não por sigilo).

- [ ] **Step 4: Confirmar** — reportar os dois valores coletados antes de seguir pra Task 2.

---

### Task 2: Schema do banco + RLS

**Files:**
- Create: `supabase/migrations/0001_init.sql`

**Interfaces:**
- Consumes: nenhuma (primeira migration).
- Produces: tabelas `profiles`, `user_progress`, `invite_codes` com RLS ativado — usadas por todas as tasks seguintes.

- [ ] **Step 1: Escrever a migration**

```sql
-- supabase/migrations/0001_init.sql

create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  is_admin boolean not null default false,
  created_at timestamptz not null default now()
);

create table user_progress (
  user_id uuid primary key references auth.users(id) on delete cascade,
  data jsonb not null,
  updated_at timestamptz not null default now()
);

create table invite_codes (
  code text primary key,
  used_by uuid references auth.users(id),
  used_at timestamptz,
  created_at timestamptz not null default now()
);

alter table profiles enable row level security;
alter table user_progress enable row level security;
alter table invite_codes enable row level security;

create policy "own profile" on profiles
  for select using (auth.uid() = id);

create policy "own progress select" on user_progress
  for select using (auth.uid() = user_id);
create policy "own progress upsert" on user_progress
  for insert with check (auth.uid() = user_id);
create policy "own progress update" on user_progress
  for update using (auth.uid() = user_id);

-- invite_codes: sem policy de select/insert/update para o client role
-- (authenticated/anon). Só as Edge Functions, usando a service_role key,
-- acessam esta tabela — service_role ignora RLS por padrão no Supabase.
```

- [ ] **Step 2: Rodar a migration no projeto Supabase**

No painel do Supabase (SQL Editor), colar o conteúdo do arquivo e executar. Alternativa (se a dona tiver Supabase CLI instalado, o que não é o caso hoje): `supabase db push`. Como não há CLI instalado ainda, seguir pelo SQL Editor da interface web.

- [ ] **Step 3: Verificar**

No SQL Editor, rodar `select * from profiles; select * from user_progress; select * from invite_codes;` — as três devem retornar "Success. No rows returned" (tabelas existem, vazias). Em Database → Tables, confirmar que as três aparecem com o ícone de RLS ativado (cadeado).

- [ ] **Step 4: Commit**

```bash
cd ~/Projetos/rclsampaio-jpg.github.io
git add supabase/migrations/0001_init.sql
git commit -m "feat(db): schema inicial (profiles, user_progress, invite_codes) com RLS"
git push origin main
```

---

### Task 3: Edge Function de cadastro (`validate-invite-and-signup`)

**Files:**
- Create: `supabase/functions/validate-invite-and-signup/index.ts`

**Interfaces:**
- Consumes: tabela `invite_codes` e `profiles` (Task 2); Supabase Auth Admin API (`service_role` key, disponível automaticamente dentro de uma Edge Function via `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')`).
- Produces: endpoint HTTP `POST /functions/v1/validate-invite-and-signup` que recebe `{ email, password, code }` e devolve `{ success: true }` ou `{ success: false, error: string }`. Consumido por `SignupView.tsx` (Task 6).

- [ ] **Step 1: Escrever a função**

```typescript
// supabase/functions/validate-invite-and-signup/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const supabaseAdmin = createClient(
  Deno.env.get('SUPABASE_URL')!,
  Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,
);

Deno.serve(async (req: Request) => {
  const corsHeaders = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, content-type',
  };
  if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders });

  try {
    const { email, password, code } = await req.json();
    if (!email || !password || !code) {
      return new Response(
        JSON.stringify({ success: false, error: 'Email, senha e código são obrigatórios.' }),
        { status: 400, headers: corsHeaders },
      );
    }

    const { data: invite, error: inviteError } = await supabaseAdmin
      .from('invite_codes')
      .select('code, used_by')
      .eq('code', code)
      .maybeSingle();

    if (inviteError || !invite) {
      return new Response(
        JSON.stringify({ success: false, error: 'Código inválido ou já utilizado.' }),
        { status: 400, headers: corsHeaders },
      );
    }
    if (invite.used_by) {
      return new Response(
        JSON.stringify({ success: false, error: 'Código inválido ou já utilizado.' }),
        { status: 400, headers: corsHeaders },
      );
    }

    const { data: created, error: createError } = await supabaseAdmin.auth.admin.createUser({
      email,
      password,
      email_confirm: true,
    });
    if (createError || !created.user) {
      return new Response(
        JSON.stringify({ success: false, error: createError?.message ?? 'Não foi possível criar a conta.' }),
        { status: 400, headers: corsHeaders },
      );
    }

    await supabaseAdmin.from('profiles').insert({ id: created.user.id, is_admin: false });
    await supabaseAdmin
      .from('invite_codes')
      .update({ used_by: created.user.id, used_at: new Date().toISOString() })
      .eq('code', code);

    return new Response(JSON.stringify({ success: true }), { status: 200, headers: corsHeaders });
  } catch (e) {
    return new Response(
      JSON.stringify({ success: false, error: 'Erro inesperado ao criar conta.' }),
      { status: 500, headers: corsHeaders },
    );
  }
});
```

- [ ] **Step 2: Deploy da função**

Pelo painel do Supabase: Edge Functions → Create a new function → nome `validate-invite-and-signup` → colar o código do arquivo → Deploy. (Sem CLI instalado; a interface web do Supabase permite colar e publicar direto.)

- [ ] **Step 3: Verificar manualmente**

No painel do Supabase, primeiro inserir um código de teste: SQL Editor → `insert into invite_codes (code) values ('TESTE123');`. Depois, em Edge Functions → `validate-invite-and-signup` → aba "Testing", enviar:

```json
{ "email": "teste@example.com", "password": "senhaTeste123", "code": "TESTE123" }
```

Expected: resposta `{"success": true}`. Conferir em Authentication → Users que `teste@example.com` foi criado, e que `select used_by from invite_codes where code = 'TESTE123'` não é mais nulo.

Repetir o mesmo request (mesmo código) — expected: `{"success": false, "error": "Código inválido ou já utilizado."}`.

Limpar o usuário de teste depois (Authentication → Users → deletar `teste@example.com`) para não sujar a base de usuárias reais.

- [ ] **Step 4: Commit**

```bash
cd ~/Projetos/rclsampaio-jpg.github.io
git add supabase/functions/validate-invite-and-signup/index.ts
git commit -m "feat(auth): edge function de cadastro com validação de convite"
git push origin main
```

---

### Task 4: Edge Function de geração de convite (`admin-generate-invite`)

**Files:**
- Create: `supabase/functions/admin-generate-invite/index.ts`

**Interfaces:**
- Consumes: tabela `invite_codes` (Task 2).
- Produces: endpoint HTTP `POST /functions/v1/admin-generate-invite` que recebe `{}` (sem corpo necessário) e devolve `{ code: string }`. Consumido por `InviteAdminPanel.tsx` (Task 7).

- [ ] **Step 1: Escrever a função**

```typescript
// supabase/functions/admin-generate-invite/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const supabaseAdmin = createClient(
  Deno.env.get('SUPABASE_URL')!,
  Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,
);

function randomCode(length = 8): string {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'; // sem O/0/I/1, evita confusão visual
  let out = '';
  for (let i = 0; i < length; i++) {
    out += chars[Math.floor(Math.random() * chars.length)];
  }
  return out;
}

Deno.serve(async (req: Request) => {
  const corsHeaders = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, content-type',
  };
  if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders });

  const code = randomCode();
  const { error } = await supabaseAdmin.from('invite_codes').insert({ code });
  if (error) {
    return new Response(
      JSON.stringify({ error: 'Não foi possível gerar o código.' }),
      { status: 500, headers: corsHeaders },
    );
  }
  return new Response(JSON.stringify({ code }), { status: 200, headers: corsHeaders });
});
```

**Nota de segurança deliberada:** esta função não checa uma senha de admin — ela só gera um código aleatório e o insere. A proteção de "quem pode chamar isso" fica no front-end (`InviteAdminPanel` só é renderizado depois do gate de `ADMIN_PASSPHRASE`), consistente com o nível de segurança que o resto do admin do app já usa (`CLAUDE.md`: "client-side deterrent, not real security"). Se no futuro isso precisar de proteção real, a function passaria a exigir um header de autorização verificado — fora de escopo agora.

- [ ] **Step 2: Deploy da função**

Mesmo processo da Task 3: Edge Functions → Create a new function → `admin-generate-invite` → colar código → Deploy.

- [ ] **Step 3: Verificar manualmente**

Aba "Testing" da função, enviar request vazio (`{}`). Expected: resposta `{"code": "XXXXXXXX"}` (8 caracteres). Conferir no SQL Editor: `select * from invite_codes order by created_at desc limit 1;` mostra o código novo com `used_by` nulo.

- [ ] **Step 4: Commit**

```bash
cd ~/Projetos/rclsampaio-jpg.github.io
git add supabase/functions/admin-generate-invite/index.ts
git commit -m "feat(admin): edge function de geração de código de convite"
git push origin main
```

---

### Task 5: Cliente Supabase + AuthContext no front-end

**Files:**
- Create: `src/lib/supabase.ts`
- Create: `src/contexts/AuthContext.tsx`
- Modify: `package.json` (adicionar dependência)
- Modify: `.env.local` (não commitado — só para dev local; ver `.gitignore`)

**Interfaces:**
- Consumes: `SUPABASE_URL`, `SUPABASE_ANON_KEY` (Task 1).
- Produces: `supabase` (cliente exportado de `src/lib/supabase.ts`), `useAuth()` hook retornando `{ user, session, loading, signIn(email, password), signOut() }`. Consumido por `App.tsx`, `LoginView.tsx`, `SignupView.tsx`, `useProgressSync.ts`.

- [ ] **Step 1: Instalar a dependência**

```bash
cd ~/Projetos/rclsampaio-jpg.github.io
npm install @supabase/supabase-js
```

Expected: `package.json` e `package-lock.json` atualizados, sem erros.

- [ ] **Step 2: Criar `.env.local` (dev local, não commitado)**

Confirmar que `.gitignore` já ignora `.env*` (checar `cat .gitignore`); se não ignorar, adicionar `.env.local` antes de criar o arquivo. Depois criar:

```bash
# .env.local — NÃO COMMITAR
VITE_SUPABASE_URL=<valor da Task 1>
VITE_SUPABASE_ANON_KEY=<valor da Task 1>
```

- [ ] **Step 3: Criar o cliente Supabase**

```typescript
// src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

- [ ] **Step 4: Criar o AuthContext**

```typescript
// src/contexts/AuthContext.tsx
import { createContext, useContext, useEffect, useState, ReactNode } from 'react';
import type { Session, User } from '@supabase/supabase-js';
import { supabase } from '../lib/supabase';

interface AuthContextValue {
  user: User | null;
  session: Session | null;
  loading: boolean;
  signIn: (email: string, password: string) => Promise<{ error: string | null }>;
  signOut: () => Promise<void>;
}

const AuthContext = createContext<AuthContextValue | undefined>(undefined);

export function AuthProvider({ children }: { children: ReactNode }) {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session);
      setLoading(false);
    });
    const { data: listener } = supabase.auth.onAuthStateChange((_event, newSession) => {
      setSession(newSession);
    });
    return () => listener.subscription.unsubscribe();
  }, []);

  const signIn = async (email: string, password: string) => {
    const { error } = await supabase.auth.signInWithPassword({ email, password });
    return { error: error ? traduzErroLogin(error.message) : null };
  };

  const signOut = async () => {
    await supabase.auth.signOut();
  };

  return (
    <AuthContext.Provider value={{ user: session?.user ?? null, session, loading, signIn, signOut }}>
      {children}
    </AuthContext.Provider>
  );
}

function traduzErroLogin(message: string): string {
  if (message.includes('Invalid login credentials')) return 'Email ou senha incorretos.';
  return 'Não foi possível entrar. Tente novamente.';
}

export function useAuth(): AuthContextValue {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within an AuthProvider');
  return ctx;
}
```

- [ ] **Step 5: Verificar (build)**

```bash
npx tsc --noEmit
npm run build
```

Expected: ambos sem erros — os novos arquivos ainda não são importados por nada, então isso só confirma que compilam isoladamente.

- [ ] **Step 6: Commit**

```bash
git add src/lib/supabase.ts src/contexts/AuthContext.tsx package.json package-lock.json
git commit -m "feat(auth): cliente Supabase e AuthContext"
git push origin main
```

(`.env.local` não entra no commit — confirmar com `git status` que não aparece antes de dar `git add`.)

---

### Task 6: Telas de Login e Cadastro (com frontend-design)

**Files:**
- Create: `src/components/auth/LoginView.tsx`
- Create: `src/components/auth/SignupView.tsx`

**Interfaces:**
- Consumes: `useAuth()` (Task 5) para login; chama diretamente `fetch` na Edge Function `validate-invite-and-signup` (Task 3) para cadastro — cadastro não usa `supabase.auth.signUp` do cliente porque a validação do código precisa acontecer no servidor antes de criar a conta.
- Produces: componentes `<LoginView onSwitchToSignup={() => void} />` e `<SignupView inviteCodeFromUrl={string | null} onSwitchToLogin={() => void} />`, montados em `App.tsx` (Task 8).

- [ ] **Step 1: Rodar a skill frontend-design para as duas telas**

Invocar a skill `frontend-design` (fora deste plano, interativamente) para definir aparência de `LoginView` e `SignupView` — tom visual, tipografia, espaçamento — consistente com o restante do app (conferir `src/index.css` e um componente existente como `ProfileView.tsx` como referência de estilo atual antes de propor). Resultado: direção visual aprovada para usar nos steps seguintes.

- [ ] **Step 2: Implementar `LoginView.tsx`**

```typescript
// src/components/auth/LoginView.tsx
import { useState } from 'react';
import { useAuth } from '../../contexts/AuthContext';

interface LoginViewProps {
  onSwitchToSignup: () => void;
}

export default function LoginView({ onSwitchToSignup }: LoginViewProps) {
  const { signIn } = useAuth();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitting(true);
    setError(null);
    const { error } = await signIn(email, password);
    setSubmitting(false);
    if (error) setError(error);
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* Estrutura visual final definida no Step 1 (frontend-design);
          campos funcionais mínimos abaixo: */}
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        placeholder="Email"
        required
      />
      <input
        type="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        placeholder="Senha"
        required
      />
      {error && <p role="alert">{error}</p>}
      <button type="submit" disabled={submitting}>
        {submitting ? 'Entrando...' : 'Entrar'}
      </button>
      <button type="button" onClick={onSwitchToSignup}>
        Não tenho conta ainda
      </button>
    </form>
  );
}
```

- [ ] **Step 3: Implementar `SignupView.tsx`**

```typescript
// src/components/auth/SignupView.tsx
import { useState } from 'react';

interface SignupViewProps {
  inviteCodeFromUrl: string | null;
  onSwitchToLogin: () => void;
  onSignupSuccess: () => void;
}

export default function SignupView({ inviteCodeFromUrl, onSwitchToLogin, onSignupSuccess }: SignupViewProps) {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [code, setCode] = useState(inviteCodeFromUrl ?? '');
  const [error, setError] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitting(true);
    setError(null);

    const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string;
    try {
      const res = await fetch(`${supabaseUrl}/functions/v1/validate-invite-and-signup`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email, password, code }),
      });
      const json = await res.json();
      if (!json.success) {
        setError(json.error ?? 'Não foi possível criar a conta.');
        setSubmitting(false);
        return;
      }
      onSignupSuccess();
    } catch {
      setError('Erro de conexão. Tente novamente.');
      setSubmitting(false);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* Estrutura visual final definida no Step 1 (frontend-design) */}
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        placeholder="Email"
        required
      />
      <input
        type="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        placeholder="Crie uma senha"
        required
        minLength={6}
      />
      <input
        type="text"
        value={code}
        onChange={(e) => setCode(e.target.value)}
        placeholder="Código de convite"
        required
      />
      {error && <p role="alert">{error}</p>}
      <button type="submit" disabled={submitting}>
        {submitting ? 'Criando conta...' : 'Criar conta'}
      </button>
      <button type="button" onClick={onSwitchToLogin}>
        Já tenho conta
      </button>
    </form>
  );
}
```

- [ ] **Step 4: Verificar (build)**

```bash
npx tsc --noEmit
npm run build
```

Expected: sem erros (componentes ainda não montados em `App.tsx`, só compilando isolados).

- [ ] **Step 5: Commit**

```bash
git add src/components/auth/LoginView.tsx src/components/auth/SignupView.tsx
git commit -m "feat(auth): telas de login e cadastro"
git push origin main
```

---

### Task 7: Painel de admin para gerar convites (com frontend-design)

**Files:**
- Create: `src/components/auth/InviteAdminPanel.tsx`

**Interfaces:**
- Consumes: chama a Edge Function `admin-generate-invite` (Task 4) via `fetch`.
- Produces: componente `<InviteAdminPanel />`, montado em `App.tsx` (Task 8) atrás do gate de `ADMIN_PASSPHRASE` já existente.

- [ ] **Step 1: Rodar a skill frontend-design para esta tela**

Definir junto com a dona a aparência do painel — provavelmente simples: um botão "Gerar convite" e uma lista/histórico dos últimos códigos gerados com botões de copiar.

- [ ] **Step 2: Implementar**

```typescript
// src/components/auth/InviteAdminPanel.tsx
import { useState } from 'react';

const SITE_URL = 'https://rclsampaio-jpg.github.io';

export default function InviteAdminPanel() {
  const [generating, setGenerating] = useState(false);
  const [lastCode, setLastCode] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);

  const handleGenerate = async () => {
    setGenerating(true);
    setError(null);
    const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string;
    try {
      const res = await fetch(`${supabaseUrl}/functions/v1/admin-generate-invite`, { method: 'POST' });
      const json = await res.json();
      if (!json.code) {
        setError('Não foi possível gerar o código.');
        return;
      }
      setLastCode(json.code);
    } catch {
      setError('Erro de conexão. Tente novamente.');
    } finally {
      setGenerating(false);
    }
  };

  const inviteLink = lastCode ? `${SITE_URL}/?codigo=${lastCode}` : null;

  return (
    <div>
      {/* Estrutura visual final definida no Step 1 (frontend-design) */}
      <button onClick={handleGenerate} disabled={generating}>
        {generating ? 'Gerando...' : 'Gerar convite'}
      </button>
      {error && <p role="alert">{error}</p>}
      {lastCode && (
        <div>
          <p>Código: {lastCode}</p>
          <p>Link: {inviteLink}</p>
          <button onClick={() => navigator.clipboard.writeText(inviteLink!)}>
            Copiar link
          </button>
          <button onClick={() => navigator.clipboard.writeText(lastCode)}>
            Copiar só o código
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Verificar (build)**

```bash
npx tsc --noEmit
npm run build
```

- [ ] **Step 4: Commit**

```bash
git add src/components/auth/InviteAdminPanel.tsx
git commit -m "feat(admin): painel de geração de convites"
git push origin main
```

---

### Task 8: Sincronização de progresso (`useProgressSync`)

**Files:**
- Create: `src/hooks/useProgressSync.ts`

**Interfaces:**
- Consumes: `useAuth()` (Task 5) para saber `user.id`; `supabase` (Task 5); `loadUserProgressFromStorage`/`saveUserProgressToStorage` (`src/data/templateData.ts`, já existentes); tipo `UserProgress` (`src/types.ts`).
- Produces: hook `useProgressSync(initialProgress: UserProgress)` retornando `{ progress: UserProgress, updateProgress: (p: UserProgress) => void }` — substitui o `updateProgress` local de `App.tsx` (Task 9).

- [ ] **Step 1: Implementar o hook**

```typescript
// src/hooks/useProgressSync.ts
import { useState, useEffect, useRef, useCallback } from 'react';
import { UserProgress } from '../types';
import { saveUserProgressToStorage } from '../data/templateData';
import { supabase } from '../lib/supabase';
import { useAuth } from '../contexts/AuthContext';

const SYNC_DEBOUNCE_MS = 2000;

export function useProgressSync(initialProgress: UserProgress) {
  const { user } = useAuth();
  const [progress, setProgress] = useState<UserProgress>(initialProgress);
  const debounceTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
  const didInitialLoad = useRef(false);

  // Ao logar: busca progresso da nuvem; se nuvem vazia e há progresso local
  // (localStorage), sobe o local uma vez (migração de quem já usava o app).
  useEffect(() => {
    if (!user || didInitialLoad.current) return;
    didInitialLoad.current = true;

    (async () => {
      const { data: cloudRow } = await supabase
        .from('user_progress')
        .select('data')
        .eq('user_id', user.id)
        .maybeSingle();

      if (cloudRow?.data) {
        const cloudProgress = cloudRow.data as UserProgress;
        setProgress(cloudProgress);
        saveUserProgressToStorage(cloudProgress);
      } else {
        // Nada na nuvem ainda: migra o progresso local (se houver) para lá.
        await supabase.from('user_progress').upsert({
          user_id: user.id,
          data: initialProgress,
          updated_at: new Date().toISOString(),
        });
      }
    })();
  }, [user, initialProgress]);

  const updateProgress = useCallback(
    (newProgress: UserProgress) => {
      setProgress(newProgress);
      saveUserProgressToStorage(newProgress);

      if (!user) return; // sem sessão ainda (não deveria acontecer pós-login, mas defensivo)

      if (debounceTimer.current) clearTimeout(debounceTimer.current);
      debounceTimer.current = setTimeout(() => {
        supabase
          .from('user_progress')
          .upsert({ user_id: user.id, data: newProgress, updated_at: new Date().toISOString() });
      }, SYNC_DEBOUNCE_MS);
    },
    [user],
  );

  return { progress, updateProgress };
}
```

- [ ] **Step 2: Verificar (build)**

```bash
npx tsc --noEmit
npm run build
```

Expected: sem erros — hook ainda não é usado por `App.tsx` (isso é a Task 9).

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useProgressSync.ts
git commit -m "feat(sync): hook de sincronização de progresso com Supabase"
git push origin main
```

---

### Task 9: Integrar tudo em `App.tsx` e remover o gate antigo

**Files:**
- Modify: `src/App.tsx`
  - Remover linhas 58–83 (bloco `ACCESS_PASSPHRASE`/`isAccessUnlocked`/etc).
  - Envolver a renderização com `<AuthProvider>`.
  - Trocar `useState<UserProgress>` + `updateProgress` local (linhas 52, 173–177) por `useProgressSync`.
  - Adicionar leitura de `?codigo=` da URL (`window.location.search`) para passar como `inviteCodeFromUrl` à `SignupView`.
  - Renderizar `LoginView`/`SignupView` quando `!user`; renderizar o app normal quando `user` existe.
  - Montar `InviteAdminPanel` na área já gated por `isAdminUnlocked` (perto de `CmsView`/`BrandIdentityView`, linha ~1094).
- Delete: `src/services/firebaseAdapter.ts`

**Interfaces:**
- Consumes: `AuthProvider`/`useAuth` (Task 5), `LoginView`/`SignupView` (Task 6), `InviteAdminPanel` (Task 7), `useProgressSync` (Task 8).
- Produces: comportamento final do app — gate de acesso agora é login real, progresso sincroniza.

- [ ] **Step 1: Remover o gate de `ACCESS_PASSPHRASE`**

Em `src/App.tsx`, deletar o bloco das linhas 58–83 (`ACCESS_REASK_INTERVAL_MS` até `handleAccessUnlock`) e qualquer JSX que renderiza a tela de senha de acesso compartilhada (buscar por `isAccessUnlocked`, `accessPassInput`, `accessPassError` no arquivo e remover todos os usos).

- [ ] **Step 2: Trocar o gerenciamento de progresso**

Substituir:
```typescript
const [progress, setProgress] = useState<UserProgress>(() => loadUserProgressFromStorage());
```
e
```typescript
const updateProgress = (newProgress: UserProgress) => {
  setProgress(newProgress);
  saveUserProgressToStorage(newProgress);
};
```
por:
```typescript
const { progress, updateProgress } = useProgressSync(loadUserProgressFromStorage());
```
(mantendo `loadUserProgressFromStorage()` como valor inicial — é o que já popula `days` na linha seguinte via `progress.journeyStartDate`, então a ordem de inicialização não muda.)

- [ ] **Step 3: Ler o código de convite da URL**

No topo do componente `App`, adicionar:
```typescript
const inviteCodeFromUrl = new URLSearchParams(window.location.search).get('codigo');
```

- [ ] **Step 4: Renderizar login/cadastro quando não autenticado**

Envolver o `export default function App()` com um wrapper que fornece o contexto, e dentro dele, condicionar a renderização:

```typescript
// No topo do arquivo, junto aos outros imports:
import { AuthProvider, useAuth } from './contexts/AuthContext';
import LoginView from './components/auth/LoginView';
import SignupView from './components/auth/SignupView';
import InviteAdminPanel from './components/auth/InviteAdminPanel';

export default function App() {
  return (
    <AuthProvider>
      <AppContent />
    </AuthProvider>
  );
}

function AppContent() {
  const { user, loading } = useAuth();
  const inviteCodeFromUrl = new URLSearchParams(window.location.search).get('codigo');
  const [authMode, setAuthMode] = useState<'login' | 'signup'>(inviteCodeFromUrl ? 'signup' : 'login');

  if (loading) return null; // ou um spinner simples, se já existir um padrão no projeto

  if (!user) {
    return authMode === 'login' ? (
      <LoginView onSwitchToSignup={() => setAuthMode('signup')} />
    ) : (
      <SignupView
        inviteCodeFromUrl={inviteCodeFromUrl}
        onSwitchToLogin={() => setAuthMode('login')}
        onSignupSuccess={() => setAuthMode('login')}
      />
    );
  }

  // ... corpo atual da função App() continua aqui, renomeado para AppContent ...
}
```

Todo o conteúdo que hoje está dentro de `export default function App() { ... }` (a partir da linha 50, exceto o que foi movido para o wrapper) passa a viver dentro de `AppContent`.

- [ ] **Step 5: Montar o painel de admin**

Próximo de onde `CmsView`/`BrandIdentityView` são renderizados (dentro do bloco `isAdminUnlocked`, por volta da linha 1094), adicionar `<InviteAdminPanel />` — por exemplo como uma nova aba/seção dentro da área admin já existente, ou logo abaixo do `BrandIdentityView`. Seguir o padrão visual já usado para separar seções admin nesse trecho do arquivo.

- [ ] **Step 6: Remover o adapter não usado**

```bash
rm src/services/firebaseAdapter.ts
```

Buscar por qualquer import de `firebaseAdapter` no projeto (`grep -rn "firebaseAdapter" src/`) — não deve haver nenhum (era código morto, nunca importado); se houver, remover o import também.

- [ ] **Step 7: Verificar (build)**

```bash
npx tsc --noEmit
npm run build
```

Expected: ambos sem erros.

- [ ] **Step 8: Verificar manualmente no navegador**

```bash
npm run dev
```

Abrir `http://localhost:3000`: deve mostrar `LoginView` (sem sessão). Abrir `http://localhost:3000/?codigo=ABC123`: deve mostrar `SignupView` com o campo código já preenchido com `ABC123`. Testar cadastro com um código real gerado na Task 4 (via `InviteAdminPanel`, depois de logar como admin com `ADMIN_PASSPHRASE`) — confirmar que login funciona depois e que o app principal carrega.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat(auth): integra login/cadastro/sync no App, remove gate antigo e adapter morto"
git push origin main
```

---

### Task 10: Variáveis de ambiente no deploy (GitHub Actions)

**Files:**
- Modify: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: GitHub Secrets `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (a criar neste task).
- Produces: build de produção com as variáveis do Supabase corretamente embutidas (hoje o workflow não injeta nenhuma env var).

- [ ] **Step 1: Criar os Secrets no repositório GitHub**

Via `gh` CLI (já autenticado neste ambiente):

```bash
export PATH="$HOME/bin:$PATH"
gh secret set VITE_SUPABASE_URL --repo rclsampaio-jpg/rclsampaio-jpg.github.io --body "<valor da Task 1>"
gh secret set VITE_SUPABASE_ANON_KEY --repo rclsampaio-jpg/rclsampaio-jpg.github.io --body "<valor da Task 1>"
```

Expected: `gh secret list --repo rclsampaio-jpg/rclsampaio-jpg.github.io` mostra as duas chaves.

- [ ] **Step 2: Injetar no step de build**

Em `.github/workflows/deploy.yml`, modificar:
```yaml
      - run: npm run build
```
para:
```yaml
      - run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
```

- [ ] **Step 3: Commit e observar o deploy**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: injeta variáveis do Supabase no build de produção"
git push origin main
```

Acompanhar a execução: `gh run watch --repo rclsampaio-jpg/rclsampaio-jpg.github.io` (ou abrir a aba Actions no navegador). Expected: workflow completo com sucesso (✓ build, ✓ deploy).

- [ ] **Step 4: Verificar em produção**

Abrir `https://rclsampaio-jpg.github.io` no navegador (Playwright) — deve mostrar a tela de login (não mais a senha compartilhada antiga). Testar login com a conta de admin real da dona (criada manualmente antes, ou via um convite gerado na Task 9 Step 8) para confirmar que o fluxo completo funciona em produção, não só localmente.

---

## Self-Review

**Cobertura da spec:**
- Backend Supabase + schema + RLS → Tasks 1–2. ✓
- Fluxo de cadastro com validação server-side do código → Task 3, 6. ✓
- Link de convite com código pré-preenchido + código solto → Task 4 (gera ambos), 6 (Signup lê `?codigo=`), 7 (painel copia ambos). ✓
- Geração de convites pela admin (gated por `ADMIN_PASSPHRASE`) → Task 4, 7, 9 (montagem). ✓
- Sincronização de progresso + migração automática do local → Task 8. ✓
- Conflito "save mais recente vence" → implícito no `upsert` com `updated_at` (Task 8) — não há lógica de merge, cada upsert sobrescreve a linha inteira. ✓
- Remoção de `ACCESS_PASSPHRASE`, manutenção de `ADMIN_PASSPHRASE` → Task 9. ✓
- `frontend-design` nas telas novas → Task 6 Step 1, Task 7 Step 1. ✓
- Deploy/CI com as novas env vars → Task 10. ✓

**Placeholders:** nenhum "TBD"/"implementar depois" — todo step tem código completo ou comando exato.

**Consistência de tipos:** `useProgressSync` (Task 8) retorna `{ progress, updateProgress }` com a mesma assinatura que `App.tsx` já usava localmente (`updateProgress(newProgress: UserProgress) => void`), então a Task 9 é uma troca direta sem mudar chamadores em outros componentes (`HomeView`, `DailyMissionView`, `JourneyView` continuam recebendo `updateProgress` como prop do jeito que já recebem hoje — não precisou tocar neles, ao contrário do que a spec original supôs).

---

**Plan complete and saved to `docs/superpowers/plans/2026-07-31-user-auth.md`.**

Duas opções de execução:

1. **Subagent-Driven (recomendado)** — um subagente novo por task, com revisão entre elas, iteração rápida.
2. **Execução inline** — executo as tasks nesta mesma sessão, em lote, com checkpoints pra revisão.

Qual prefere?
